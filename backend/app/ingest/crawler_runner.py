"""Crawler runner (stub for demo).
In the real project, this module will:
- fetch new documents from a public source
- normalise to Document schema
- call /admin/ingest (or directly DocumentStore + update_index)
"""
